# Electron native modules must be built against Electron's ABI, not Node's.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
# Lock transitive versions for reproducible + auditable builds (§13.1)
resolution-mode=highest
